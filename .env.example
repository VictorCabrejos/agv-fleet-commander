# Process environment reference only; no .env loader is used.
AGV_STATE_PATH=.runtime/fleet.sqlite3
AGV_PLANNER=heuristic
# Optional only: set AGV_PLANNER=openai and AGV_OPENAI_MODEL in your shell.
# Supply OPENAI_API_KEY through your local secret environment, never this file.
